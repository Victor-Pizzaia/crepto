const ping = require('./ping');
const coinPrice = require('./value');
const currencies = require('./currencies');
const calc = require('./calc');

module.exports = {
  ping,
  coinPrice,
  currencies,
  calc,
};
